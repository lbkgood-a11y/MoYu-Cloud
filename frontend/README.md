# MoYu-Cloud 管理端

当前使用 Vue 3、TypeScript、Vite 和 Element Plus 实现 MVP 管理端原型，后续接入 RuoYi-Vue3 的成熟布局与权限能力。

```bash
npm install
npm run dev
```

开发服务器默认地址：`http://localhost:5173`。通过 Vite 代理访问本地后端 `http://localhost:8080`。
